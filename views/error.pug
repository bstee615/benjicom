extends base

block header

block content
  div(style="padding-right: 20px")
    h1= error.status + ' ' + message
    a(href="/", style="display: block; border: none; margin: 10px") Back to Home
  pre #{error.stack}
